{% trans_default_domain 'user.profile' %}

{# Имя профиля #}
{{ form_row(form.personal.username, {  label : 'form.label.username'|trans }) }}

{# URL Адрес профиля #}
{{ form_row(form.info.url, {  label : 'form.label.url'|trans }) }}

{# Сортировка #}
{{ form_row(form.sort, { label: 'form.label.sort'|trans }) }}